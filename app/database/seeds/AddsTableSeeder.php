<?php

class AddsTableSeeder extends Seeder {

    public function run()
    {
    	// Uncomment the below to wipe the table clean before populating
    	// DB::table('adds')->delete();

        $adds = array(

        );

        // Uncomment the below to run the seeder
        // DB::table('adds')->insert($adds);
    }

}